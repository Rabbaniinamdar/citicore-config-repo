# ─────────────────────────────────────────────────────────────
# auth-service.yml — OVERRIDES ONLY
# Everything not listed here is inherited from application.yml
# ─────────────────────────────────────────────────────────────

server:
  port: 8081

spring:
  application:
    name: auth-service

  # ─── Database (DEV: H2) ───────────────────────────────────
  datasource:
    url: jdbc:h2:mem:authdb;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=10000
    driver-class-name: org.h2.Driver
    username: sa
    password: password

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true
      path: /h2-console

  # auth-service only PRODUCES (otp-topic) — no consumer group needed
  kafka:
    consumer:
      group-id: auth-group

logging:
  level:
    com.citicore.auth: DEBUG

# ─── PROD override (use Spring profiles) ─────────────────────
# spring:
#   datasource:
#     url: jdbc:postgresql://localhost:5432/citicore_auth
#     driver-class-name: org.postgresql.Driver
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
#   jpa:
#     database-platform: org.hibernate.dialect.PostgreSQLDialect
#     hibernate:
#       ddl-auto: validate
#     show-sql: false