# ─────────────────────────────────────────────────────────────
# user-service.yml — OVERRIDES ONLY
# Everything not listed here is inherited from application.yml
# ─────────────────────────────────────────────────────────────

server:
  port: 8082
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB

spring:
  application:
    name: user-service

  # ─── Database (DEV: H2) ───────────────────────────────────
  # ─── Database (DEV: H2) ───────────────────────────────────
  datasource:
    url: jdbc:mysql://localhost:3306/userdb
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  # user-service only PRODUCES (kyc-topic) — no consumer needed
  kafka:
    consumer:
      group-id: user-group

# ─── KYC local upload dir (dev fallback) ─────────────────────
kyc:
  upload-dir: uploads/kyc/

logging:
  level:
    com.citicore.user: DEBUG

# ─── PROD override ────────────────────────────────────────────
# spring:
#   datasource:
#     url: jdbc:postgresql://localhost:5432/citicore_user
#     driver-class-name: org.postgresql.Driver
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
#   jpa:
#     database-platform: org.hibernate.dialect.PostgreSQLDialect
#     hibernate:
#       ddl-auto: validate
#     show-sql: false